@page
@model PhoneDB.Pages.Section.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Section</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Section.Phone)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Section.Phone.Id)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Section.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
